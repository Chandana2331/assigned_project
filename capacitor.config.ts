import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'codeeasy.example.com',
  appName: 'code-easyapp',
  webDir: 'dist/assigned_project'
};

export default config;
